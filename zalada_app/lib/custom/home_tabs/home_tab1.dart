import 'package:flutter/material.dart';

class Home_Tab1 extends StatelessWidget {
  const Home_Tab1({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Home Tab1'),
    );
  }
}
